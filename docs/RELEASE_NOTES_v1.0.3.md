# Cricket Intelligence API v1.0.3

P0 reconciliation patch: updates the transitive `brace-expansion` dependency to the fixed release and uses the canonical public repository path in the root test report. Runtime behavior is unchanged from v1.0.2.
